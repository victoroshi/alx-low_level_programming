#include <stdio.h>

/**
 * main - Entry point
 * Description: Using printf to print charaters
 * Return: always 0 on success
 */
int main(void)
{
	char myString[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", myString);

	return (0);
}

