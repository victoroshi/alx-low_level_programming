#include <stdio.h>

/**
 * main - Entry point
 * Description: Program to display character to the standard output
 * Return: always 0 on success
 */
int main(void)
{
	char myString[] = "\"Programming is like building a multilingual puzzle";

	puts(myString);

	return (0);
}

