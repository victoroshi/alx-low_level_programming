#include "main.h"

/**
 * add - adds two integers and returns the result1~
 * Description:  a function that adds two integers and returns the result.
 * @i: first integer
 * @j: second integer
 * Return: Always 0
 */
int add(int i, int j)
{
	return (i + j);
}
