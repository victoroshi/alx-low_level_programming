#include <stdio.h>
#include <ctype.h>
/**
 * mul - multiply 2 integers
 *
 * Description: a function that multiply 2 integers
 *
 * @a: first integer
 * @b: second inetger
 *
 * Return: (1) if c is digit, otherwise return (0)
 */
int mul(int a, int b)
{
	return (a * b);
}
