#include "main.h"

/**
 * mul - multiply two integer values
 * @a: char one
 * @b: char two that will be multplied with one
 * Return: return the result
 */
int mul(int a, int b)
{
	return (a * b);
}
